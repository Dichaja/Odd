name: FTP Deploy

on:
  push:
    branches: [ main ]  # Only targeting the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📂 Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 1  # Only fetch the most recent commit for speed

    - name: 📤 FTP Deploy
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.zzimbaonline.com
        username: zzimba@newzzimba.zzimbaonline.com
        password: Syscontrol.1##
        port: 21
        protocol: ftps  # Use FTPS
        security: loose  # Changed from "explicit" to "loose"
        local-dir: ./
        server-dir: /
        dangerous-clean-slate: false
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          README.md
          .github/**
        state-name: .ftp-deploy-sync-state.json

